package com.example.coursescheduler.Service;

import com.example.coursescheduler.persistence.Database;
import com.example.coursescheduler.persistence.ICoursePersistence;
import com.example.coursescheduler.persistence.ISchedulePersistence;
import com.example.coursescheduler.persistence.IStudentPersistence;

public class Service {

    /*
    private static ICoursePersistence coursePersistence = null;
    private static IStudentPersistence studentPersistence = null;
    private static ISchedulePersistence schedulePersistence = null;

    public static ICoursePersistence getCoursePersistence() {
        if (coursePersistence == null) {
            coursePersistence = new Database();
        }
        return coursePersistence;
    }

    public static IStudentPersistence getStudentPersistence() {
        if (studentPersistence == null) {
            studentPersistence = new Database();
        }
        return studentPersistence;
    }

    public static ISchedulePersistence getSchedulePersistence() {
        if (schedulePersistence == null) {
            schedulePersistence = new Database();
        }
        return schedulePersistence;
    }

     */
}